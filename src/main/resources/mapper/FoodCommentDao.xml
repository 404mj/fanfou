<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.chinamobile.sd.dao.FoodCommentDao">

    <insert id="addComment">
        insert into food_comment(content, comment_time, restaurant , discusser) VALUES
        (#{content},#{day},#{restaurant},'');
    </insert>

    <select id="findDiscussContentsByTime" resultType="com.chinamobile.sd.model.FoodComment">
        select * from food_comment
        where
        comment_time = #{day} and restaurant=#{restaurant}
        order by comment_id;
    </select>
</mapper>